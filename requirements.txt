# Runtime dependencies — exact pins for reproducible builds
# Managed manually; transitive deps are resolved by pip at install time.

# Web framework & server
fastapi==0.135.3
uvicorn[standard]==0.44.0

# Data validation
pydantic==2.13.1
pydantic-settings==2.13.1

# Database ORM
SQLAlchemy==2.0.49

# LangChain ecosystem
langchain-core==1.3.0
langchain-community==0.4.1
langchain-openai==1.1.14
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2
langchain-experimental==0.4.2

# Vector store
chromadb==1.5.8

# Embeddings (HuggingFace local)
sentence-transformers==5.4.1
torch==2.6.0

# PDF ingestion
pypdf==6.10.2

# Environment & config
python-dotenv==1.2.2

# Structured logging
structlog==25.1.0
# Prometheus metrics
prometheus-client==0.21.1

# Retry & resilience
tenacity==8.5.0

# Token counting
tiktoken==0.12.0

# HTTP
httpx==0.28.1
requests==2.33.1

# Auth & rate limiting
pyjwt==2.10.1
slowapi==0.1.9

# PII redaction
presidio-analyzer==2.2.35
presidio-anonymizer==2.2.35

# ASGI middleware (CORS, etc.)
# (included via Starlette which ships with FastAPI)
